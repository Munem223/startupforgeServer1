# StartupForge Server

Express 5 + MongoDB + Better Auth + JWT HTTP-only cookie + Stripe + ImageBB.

## Run

```bash
cp .env.example .env
npm install
npm run seed:admin
npm run seed:demo
npm run dev
```

Read the root `MASTER-SETUP-GUIDE.md` for all environment and deployment instructions.
